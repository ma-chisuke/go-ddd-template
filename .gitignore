# ビルド成果物
/out/
*.exe
*.test

# カバレッジ
*.out
coverage.*

# エディタ / OS
.DS_Store
.idea/
.vscode/

# 環境変数（秘密情報を含みうる）
.env
.env.local

# 注意: ogen / sqlc の生成コードはコミットする（.gitignore しない）。
# 生成物をリポジトリに含めることで、生成器が無い環境でもビルドでき、
# 差分レビューで契約変更の影響を確認できる。
