# フィクスチャ契約の ogen 生成設定。openapi.yaml と対で管理する。
# サーバ実装だけを生成する（テストは httptest でサーバへ HTTP を投げ、ogen が実際に
# 生成するエラーを捕まえる）。テンプレートを軽く保つため OpenTelemetry 計装も無効化する。
generator:
  features:
    disable:
      - "paths/client"
      - "ogen/otel"
