-- 注文コンテキストの dev/test 専用フィクスチャ（fixtures）。
--
-- 重要: このファイルは開発・デモ・テスト環境でのみ適用する（APPLY_FIXTURES=true のとき）。
-- 本番の適用経路には決して含めない。
--
-- 注文は「作成 → 予約 → 確定」というフローで作られるトランザクションデータであり、
-- 事前投入したデモ注文は在庫側の予約状態と整合しないため、既定では投入しない。デモの
-- 注文フローは、起動後に公開 API（POST /orders）を叩くか、`go run ./cmd/dev` で確認する。
--
-- 出荷（shipments）も同じ理由で投入しない。出荷は confirmed の注文に対してのみ準備できる
-- ため、事前投入した出荷は「事前投入していない注文」を指すことになり整合しない。デモの
-- 出荷フローは、注文を作ってから POST /shipments を叩く（順序に意味がある）。
--
-- デモ注文を「表示用」に事前投入したい場合は、ここに ON CONFLICT DO NOTHING で冪等に
-- 追加する（在庫の予約とは独立した、照会 UI 確認用のダミーとして扱う）。

-- （現時点ではフィクスチャなし。デモの注文・出荷フローは API / cmd/dev で実行する）
